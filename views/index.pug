extends layout

block content
    h1=title
    table
        tr
        each val, index in c[0]
            th= index
        -var n = 0
        while n<10
            tr
            each val, index in c[n++]
                if index==updates
                    td=JSON.stringify(val)
                else
                    td=val





